create Problem instance by using FileParser, so we have Problem object with all defined teams, timeslots and constraints

create Solver instance and pass the reference to the Problem, so we know what we're working on
Solver solver = new Solver(problem)
solver.solve()

save results to csv file
- for GA: population;best;average;worst;
- for SA: overallLoop;overallBest;current;

constraints[0] is always the basicConstraint (the basic assumptions of the problem)
if phased, constraints[1] is phasedConstraint
both returning only true/false

TODO:
    Save iteration info to .csv

Sparametryzować!!!

Ilość iteracji z góry określona! Temperatura mierzona przy okazji.

Tylko 4 parametry
Iteracje, Sąsiedzi, TempP, Cooling Rate
cooling rate : czwarte, piąte miejsce po przecinku
Na wykresie SA najlepszy, najgorszy, średni sąsiad.

proporcjonalne sąsiedztwo

basic in filename means no adaptive neighbourhood

-----------------------------------------
DONE Adaptacyjne - z pokolenia na pokolenie: rankingowe: czyli każdemu daję po jednym miejscu, a potem rozdzielam na bazie performance'u miejsca (czyli najlepszy w iteracji dostanie w nagrode więcej miejsc, drugi najlepszy trochę mniej itp.)
DONE Dodać na wykresie które sąsiedztwo pracowało - wygrało ranking

DONE Puścić z losowym rankingiem - jako referencyjne wyniki
DONE Sprawdzić - czy zawsze zostają złamane te same sąsiedztwa!!!